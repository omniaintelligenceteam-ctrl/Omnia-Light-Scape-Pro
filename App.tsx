function App() {
  return (
    <div
      style={{
        minHeight: "100vh",
        backgroundColor: "#111111",
        color: "#ffffff",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "system-ui, -apple-system, BlinkMacSystemFont, sans-serif",
        fontSize: 32,
      }}
    >
      🚀 LightScape Pro is LIVE
    </div>
  );
}

export default App;
